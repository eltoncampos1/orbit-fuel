## Codebase Patterns
- Calculator functions use `floor/1` (Elixir built-in) for integer truncation
- Travel domain lives in `lib/orbita_fuel/travel/`
- `total_fuel/2` uses reverse accumulation: process steps in reverse, each step's mass = base_mass + accumulated fuel from later steps (rocket equation)
- Step uses `Ecto.Enum` for :action and :planet fields within an `embedded_schema`

# Ralph Progress Log
Started: Fri Apr 17 22:32:12 -03 2026
---

## 2026-04-17 - US-007
- Created Calculator unit tests covering all functions and edge cases (12 tests, all passing)
- Files changed: test/orbita_fuel/travel/calculator_test.exs (created), prd.json
- **Learnings for future iterations:**
  - Test exact chain values (e.g. `launch Earth 28801` → `[11829, 4839, 1960, 774, 285, 84, 1]`, total=19772)
  - `use ExUnit.Case, async: true` works fine for pure Calculator tests
---

## 2026-04-17 - US-006
- Added ExCoveralls dependency and configuration to mix.exs
- Files changed: mix.exs, mix.lock, prd.json
- **Learnings for future iterations:**
  - `preferred_cli_env` must use keyword list syntax (not map) in mix.exs project/0
---

## 2026-04-17 - US-005
- Implemented `OrbitaFuel.Travel.Flight` embedded schema with mass(:float), embeds_many(:steps), changeset with validate_number and validate_length
- Files changed: lib/orbita_fuel/travel/flight.ex (created), prd.json
- **Learnings for future iterations:**
  - `cast_embed(:steps, required: true)` handles embedded step validation and propagates errors
  - `validate_length(:steps, min: 1)` ensures at least one step
---

## 2026-04-17 - US-004
- Implemented `OrbitaFuel.Travel.Step` embedded schema with Ecto.Enum fields, changeset, gravity/1, planet_label/1, action_label/1
- Files changed: lib/orbita_fuel/travel/step.ex (created), prd.json
- **Learnings for future iterations:**
  - Step was created as a prerequisite for US-003 (total_fuel/2 requires Step.gravity/1)
  - `embedded_schema` with `Ecto.Enum` values: `[:launch, :land]` and `[:earth, :moon, :mars]`
---

## 2026-04-17 - US-003
- Implemented `fuel_for_step/3` and `total_fuel/2` using reverse accumulation (rocket equation)
- Files changed: lib/orbita_fuel/travel/calculator.ex, prd.json
- **Learnings for future iterations:**
  - `total_fuel/2` uses REVERSE accumulation: last step uses base mass; each prior step's mass = base + all later step fuels
  - The spec note "original spacecraft mass" is misleading — it means base mass, but accumulated later-step fuel IS added
  - Verified: Apollo 11 = 51898, Mars Mission = 33388, Passenger Ship = 212161
---

## 2026-04-17 - US-002
- Implemented `fuel_chain/3` using `Stream.unfold/2`; seed is original mass, next accumulator is each fuel value, stops when `base_fuel <= 0`
- Files changed: lib/orbita_fuel/travel/calculator.ex, prd.json
- **Learnings for future iterations:**
  - `Stream.unfold(seed, fn current -> {emit_value, next_accumulator} | nil end)` — both the emitted value and next seed are the fuel value
  - Verified against spec: `fuel_chain(28801, 9.807, :land)` → `[9278, 2960, 915, 254, 40]`
---

## 2026-04-17 - US-001
- Implemented `OrbitaFuel.Travel.Calculator.base_fuel/3` with clauses for :launch and :land
- Files changed: lib/orbita_fuel/travel/calculator.ex (created), prd.json
- **Learnings for future iterations:**
  - Use `floor/1` (Elixir built-in) for integer truncation — no `Float.floor` needed
  - The travel domain directory `lib/orbita_fuel/travel/` was created fresh; all domain modules go here
---
